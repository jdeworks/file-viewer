# Known-view enhancement backlog — language/code viewers that MISSED the Jun-2026 enhance pass.
# One viewer id per line (dir under docs/types/*/known/<id>/). The self-paced loop drains this:
# enhance the viewer's parser+render to capture real structure (typed langs -> typed signatures;
# dynamic langs -> param names/arity + real constructs), add a node unit test, then DELETE its
# line here and commit. Empty (no id lines) = done; loop stops. Bar = arduino-sketch / solidity-lang.
# Check remaining: node tests/check-known-backlog.mjs
wren-lang
zig-lang
zsh-script
