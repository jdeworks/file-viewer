# File Viewer v0.1.0 Public-Beta Readiness Plan

## Goal

Produce an evidence-backed go/no-go decision for the File Viewer v0.1.0 public beta by exercising the browser app, its complete offline lifecycle, and the optional Companion end to end; fixing release-blocking and high-impact defects; adding focused regressions; synchronizing generated artifacts; and reporting residual risk without tagging, publishing, pushing, or changing remote settings.

## Resume checkpoint — 2026-07-11

This task is still in execution; do not treat the draft report's current GO line or old final-gate
counts as authoritative. Product/sample/parser progress is checkpointed through commit `4f248bb0`
(`Report every MIDI instrument used`). The readiness documents and final evidence are still work
in progress.

- The checked-in asset/service-worker version is `86da1cabc43c` for 5,086 manifest assets,
  but it is stale after the latest source/fixture changes. Regenerate it only after the product tree
  settles, then repeat the exact-manifest offline 11/11 run from that eventual final tree.
- The empty public 7z, audio-only MP4, invalid/no-pixel FITS, broken ELF, header-only NetCDF, no-pixel
  DICOM, placeholder APK, flat comic, two-note MIDI, CA-only PDB, zero-hash torrent, and corrupted
  PEM were replaced or strengthened. Seven new generators reproduced byte-identical output across
  two consecutive runs. `release-readiness-fixtures` passes 45/45, including the real four-entry 7z.
- Independent readers accepted the new corpus: `file`, bsdtar, unzip, readelf, OpenSSL,
  libnetcdf/netCDF4, pydicom pixel decoding, Mido, bencodepy canonical round-trip, Pillow PNG
  decoding, cryptography signature verification, and Androguard AXML/DEX parsing. The APK is an
  honest unsigned structure fixture, not a claim of Android installability; its ELF files are ABI
  fixtures, not fully linked native libraries. The DICOM is a coherent Part 10 demo, not a claim of
  complete CT-IOD conformance.
- The full Examples catalog (1,115 entries), simple-type area, and the corrected binary/media-3D
  browser areas are green. The PDB harness now proves both cartoon and stick representations change
  pixels and remain nonblank. FITS visual readiness no longer falsely equates colored metadata with
  rendered astronomy pixels.
- The adversarial parser/claim follow-up is implemented in three focused commits. Torrent and
  NetCDF use shared bounded parsers with child-process timeout regressions and signed BYTE/SHORT
  checks; a 20,000-case deterministic malformed-input sweep terminated cleanly. FITS uses a shared
  quote-aware card parser; MIDI integrates tempo maps; DICOM uses only the real Slice Thickness tag.
  APK/AAB/XAPK inventory is layout-aware and distinguishes unverified signature artifacts from a
  trust verdict. PEM reports date-window status and self-issued identity while explicitly declining
  signature/chain verification. Exact semantic/trust regressions and the affected simple, binary,
  media/3D and mobile Chromium paths pass.
- Independent read-only re-review then found and verified two additional semantic edges: invalid
  ASN.1 certificate dates now produce a neutral `Invalid/unknown validity dates` badge instead of a
  green date-window verdict, and MIDI Type 2 tracks now keep independent tempo maps and report the
  longest pattern without track-order dependence. Exact regressions pass in both track orders.
- That deeper audit continued across every adjacent surface. PEM renderer and metadata now share the
  same bounded ASN.1 decoder; exact malformed-length child-process repros terminate, calendar dates
  and validity ordering are strict, and self-issued status requires original DER Name equality.
  MIDI enforces four-byte VLQs plus track/event bounds, rejects declared-but-missing tracks, supports
  SMPTE/drop-frame timing, reports independent Type 2 duration and every program actually used per
  channel. DICOM no longer copies/scans the full pixel tail. NetCDF recognizes the streaming record
  sentinel and parses up to 1,024 variables while rendering an explicit first-100 summary. APK v1
  evidence rejects nested `META-INF` decoys. Exact focused regressions pass for each finding.
- BitTorrent now distinguishes validated v1, BEP 52 v2, and hybrid layouts; uses bounded v2 file-tree
  inventory, emits exact SHA-1 `btih` and/or SHA-256 `btmh` topics as applicable, and suppresses a
  usable-looking magnet for malformed/truncated v2 metadata. Its help page is synchronized. A
  deterministic 70,000-case sweep across torrent, NetCDF, PEM parser+metadata, MIDI, DICOM, and FITS
  terminated with every call below 4 ms in the independent review process.
- The richer comic fixture's page header collision was corrected and its readiness case now captures
  the nested scroller bottom so page 10 is visually proved rather than merely counted in the DOM.
  FITS now states in the preview itself that image-plane pixels are not decoded or rendered; the
  simple-types Chromium area verifies that disclosure. The APK example documentation now limits its
  persistent claim to structure/checksum facts covered by repository tests.
- A provisional 45/45 format capture completed, but it predates these final changes and reported 20
  hash-bound manual reviews pending. It and other superseded captures/logs were deleted: untracked
  artifacts dropped from 204 files/13 MB to 32 files/452 KB. The retained set is the hard-to-repeat
  packaged desktop/tray/native-picker evidence plus the TopoJSON harness fixture. A bounded comic
  loading/resource fix is still in progress in a disjoint lane. Do not merge or cite old visual
  evidence as final. Next, finish/review that fix, regenerate the manifest,
  recapture all 45 cases (including the new comic nested-bottom shot), individually inspect and bind
  updated judgments, and rerun renderer visuals; then repeat
  exact-manifest offline, Companion, privacy, focused and full `./scripts/check.sh` gates; reconcile
  all report/evidence counts and hashes; obtain the final independent audit; remove temporary
  validators/extractions and the obsolete readiness-only tar; and verify no owned process/port is
  left. Before final cleanup, remove `/tmp/file-viewer-fixture-validators`,
  `/tmp/file-viewer-apk-check`, `/tmp/file-viewer-py7zr`, the extracted manifest, and temporary
  preflight logs. Do not tag, push, publish, install system-wide, or change remotes.

## Requirements

- Follow `AGENTS.md`, the linked global `YAMS.md`, and the planning protocol.
- Use subagents for genuinely disjoint discovery, coding, security review, and final visual review. Keep offline/service-worker/security and Companion/security work on the strongest available reasoning lane per `MODEL_ROUTING.md`.
- Establish the risk-weighted coverage matrix before running tests.
- Manually/visually trial at least 30 representative formats and at least 20% of the 144 registered base types, with real rendered-content inspection rather than crash-only checks.
- Cover text/data, images, office/PDF, archives/e-books, media, fonts, 3D/CAD/GIS/science, binaries, and developer/config formats.
- Cover intake, examples, folder tree, search, metadata, source, editing, download/export, compare/diff, settings, keyboard interaction, responsive layout, and light/dark themes.
- Exercise 1440x900, 1280x720, and 390x844 viewports.
- Exercise cache-on-use, the complete Save for offline modal, limited and full saves, selected-viewer success, unselected-viewer offline miss, reload/navigation, status/badge behavior, stale cleanup, service-worker version change, and zero off-origin browser operation in fresh isolated contexts.
- Build and exercise the Companion with locked dependencies, disposable configuration/token/watched folders, complete lifecycle and file workflows, deletion protections, token/CORS rejection, loopback-only traffic, logs, and browser UI states.
- Attempt the packaged desktop/tray/native-picker path where the environment permits and explicitly report any gap.
- Fix release-blocking/high-impact defects found, with a focused regression for each fix.
- Finish with `./scripts/check.sh`, locked Rust tests, Companion E2E, screenshot review, and relevant artifact checks.
- Deliver the coverage matrix, visual evidence and observations, defects/fixes/regressions, exact validation outcomes, remaining gaps/risks, and a clear go/no-go recommendation.
- Do not create or push a tag, publish a release, push branches, or change remote repository settings.

## Assumptions

- Baseline commit, branch, worktree state, tools, fixture hashes, and port ownership will be re-captured as execution evidence; the values in the draft report are only the planning-time snapshot.
- This is outside built-in plan mode, so the repository's explicit sign-off gate applies. No tests, builds, product edits, or generated-artifact changes start until the revised file-backed plan is presented and the user answers “execute now.”
- Playwright's bundled Chromium is the chosen automated browser, not an authority for all browser engines. Contexts will use isolated temporary profiles and explicitly cleared storage/cache state; lack of Firefox/WebKit coverage remains a reported limitation unless time/environment permit supplemental trials.
- The host is Linux x86_64 under WSL2. WSLg may permit the Linux Tauri path, but packaged desktop/tray behavior is only counted as tested if a real process and UI can be launched and inspected.
- All destructive Companion operations will target a newly created temporary directory and temporary config only. No real user folder is in scope.
- Every planned counted sample is under `docs/examples/`, but no row counts until preflight proves format validity, expected base-type detection, meaningful content, and an effective renderer rather than a misleading fallback or blank/partial fixture.
- Generated files may change during validation. Only changes explained by source/fix work are retained, and all generated artifacts are checked again at the end.
- The binding `YAMS.md` workflow has been followed at planning time: its global symlink was read and relevant project/user memory was retrieved with the YAMS CLI. Durable implementation decisions will be captured and non-destructive repository-memory maintenance run after meaningful code changes. Retrieved memories remain contextual and are checked against current repository evidence.

## Plan

1. **Pass the execution sign-off gate.** Present this revised plan and report matrix, then wait for the user to choose “execute now” or “keep planning.” Purpose: comply with the repository's outside-plan approval protocol. Expected result: explicit execution authorization after the user has seen the reviewed scope.
2. **Freeze the baseline and evidence layout.** Re-record commit, branch, dirty state, toolchain/browser availability, port 7700 ownership, registered type count, test entry points, YAMS retrieval, and artifact destinations. Purpose: make every later claim attributable to a known checkout and environment. Expected result: a reproducible baseline and environment ledger in `V0.1.0_PUBLIC_BETA_READINESS.md`.
3. **Preflight fixtures and lock the numerator.** Hash all 45 samples, validate their container/signature where applicable, run detection without crediting a pass, and record expected/detected base type, known-file enhancement, effective renderer, content richness, and planned visual landmark. Replace inadequate fixtures before the trial without reducing family/risk coverage. Purpose: prevent fallback, blank, tiny, or enhanced-plugin-only rows from inflating coverage. Expected result: a credible 45-row candidate set; only visually meaningful successful rows later enter the numerator.
4. **Assign disjoint lanes and integration ownership.** Use separate subagents for (a) browser/fixture/visual harness work, (b) offline/service-worker/security code and tests, and (c) Companion/server/security code and tests; root owns integration, destructive-operation safety, and the report. Security-sensitive changes and the final screenshot corpus receive independent read-only review. The agent interface exposes no tier selector, so security/offline/Companion lanes use the strongest available agents and the routing rationale is recorded. Purpose: satisfy the requested parallel specialist review without overlapping file ownership. Expected result: explicit ownership and review evidence for every change.
5. **Run baseline technical validation.** Run focused static/unit checks first, then relevant browser smoke partitions to expose current failures without weakening expectations. Capture exact command, exit code, duration, request ledger, and generated-file drift. Purpose: distinguish baseline defects from later regressions. Expected result: an initial reproducible defect list.
6. **Build a deterministic visual-trial harness and inspect its output.** Drive the real intake UI for all preflighted samples, assert detected/effective renderer and type-specific meaningful landmarks, capture full-page and preview screenshots after stable render, and record page/console/request failures. Exercise a balanced viewport/theme grid across the broader product; the offline modal gets the full 3×2 grid. Write a machine-readable evidence index containing sample SHA-256, viewport, theme, detector ranking, effective renderer, landmarks, screenshot path, request/error ledger, technical verdict, visual verdict, concise observation, and reviewer disposition. Assemble contact sheets only as an aid; retain and individually inspect primary shots. Purpose: prove rendered quality. Expected result: at least 30 meaningful visual passes and 29 distinct successful base types, with the planned target of 45/144.
7. **Exercise offline behavior in fresh isolated contexts.** Prove cache-on-use first, including immediate first-visit activation/save race. Inspect the modal at all six viewport/theme combinations at top and bottom scroll, with exact preset/bundle/size oracles, keyboard semantics, focus cycle/return, emulator-hidden and enabled states. Verify exact selected-versus-cached sets for a limited selection; selected Office viewers must work after hard-offline reload and an omitted vendor-backed viewer must show the intended miss without a false whole-app-ready claim. Inject a selected-asset failure and mid-save interruption, prove no false success and accurate resume, then enable emulators and verify a true full save byte-for-byte against every manifest asset. Clear Cache Storage while retaining localStorage, test badge/navigation/reload, and use a temporary mutable same-origin v1→v2 server to prove waiting-banner dismiss/reload, controller takeover, unrelated-cache preservation, stale File Viewer cache cleanup, and honest saved state. Audit exact parsed origins for every page/context, service-worker/background request, WebSocket, and fetch-capable sanitized format. Purpose: validate trust/offline promises without contamination. Expected result: complete matrix evidence or a blocker/high-impact defect.
8. **Exercise the Companion against disposable state.** Build with `cargo build --offline --locked --manifest-path companion/Cargo.toml`; create a temp config/token/root/log set and refuse to proceed if port 7700 is occupied. Deterministically delay/fail health calls to capture absent, disabled, connecting, connected, disconnected, and reconnecting states; prove duplicate ownership. Exercise Settings enable/test, add/remove watched roots at runtime and verify watcher subscribe/unsubscribe, browse/open/auto-link/edit/save/create, real external watcher updates, logs, and download/export. Test payloads below/above 2 MiB, block save-back for truncated inputs, and verify temp-file/replace behavior. Delete a file and nested folder via confirmations; prove exact watched-root and nested-watched-root protection. Probe symlink/traversal/out-of-root/finder bounds and config persistence failure. Run a request ledger allowing only viewer origin and `127.0.0.1:7700`; for hostile/lookalike/`null` origins and preflights, prove rejection and unchanged filesystem, separately from missing/wrong/correct token tests. Inspect the bound socket. Purpose: validate the opt-in local write boundary and complete workflow. Expected result: only loopback Companion traffic and changes inside the temp root, with all requested UI evidence.
9. **Attempt packaged desktop behavior.** Check prerequisites; run `cd companion/src-tauri && cargo tauri build --bundles deb -- --locked`, install/launch the built package when safe, and require a real UI/tray/native-picker/quit/second-instance interaction against the disposable root before crediting coverage. If the environment cannot launch a real packaged path, collect the exact blocker and mark it as the sole permissible environment-only untested exception; never infer runtime coverage from source/build success. Purpose: keep platform claims honest. Expected result: direct evidence or a precisely bounded release risk.
10. **Iterate defect → regression → retest.** When a blocker invalidates dependent evidence, stop those trials, reproduce narrowly, assign non-overlapping fixes to the appropriate lane, add a regression that fails before/passes after, obtain independent security review where relevant, and rerun every affected matrix row before continuing. Purpose: avoid a late repair batch leaving stale evidence. Expected result: a defect table linking symptom, root cause, files, regression, review, and fresh visual/technical evidence.
11. **Regenerate and run exact final gates.** Run the readiness fixture/visual/offline/Companion harnesses; `node tests/markdown-remote-resources.test.mjs`; `node tests/asset-manifest.test.mjs`; `cargo test --offline --locked --manifest-path companion/Cargo.toml`; `node tests/companion-e2e.mjs`; and full `./scripts/check.sh`. To preserve the user's real index while satisfying the gate's staged-artifact semantics, create a temporary `GIT_INDEX_FILE`, populate it from HEAD, stage the final working tree only into that temporary index, and run the full gate with that index. Re-run generators/checks to fixed point and compare checksums/diffs for `docs/core/app.generated.js`, registry/detector/settings outputs, examples summaries, `docs/asset-manifest.json`, and `docs/sw.js`. Purpose: verify the final tree and synchronized artifacts. Expected result: exact outcomes, no unexplained generator drift, and no mutation of the real Git index.
12. **Audit completion and issue the decision.** An independent read-only reviewer reinspects every primary screenshot/evidence record. Resolve each matrix row to pass/fail/gap and verify every request requirement. Any failed final gate, default-mode off-origin request, offline critical-path failure, Companion destructive/auth/path-boundary failure, unresolved blocker/high-impact defect, missing mandatory visual evidence, or failure to meet both coverage floors forces **no-go**. The sole non-automatic gap is an environment-only packaged desktop path that was genuinely attempted and precisely reported; its severity still informs the recommendation. Purpose: ensure the recommendation follows evidence. Expected result: a self-contained, defensible go/no-go report.

## Validation

- Registry denominator: import `REGISTRY` from `docs/core/registry.js` and verify exactly 144 unique entries at execution time. Count only distinct expected/detected IDs with valid fixtures, effective renderers, meaningful content, and completed individual visual inspection; require both at least 29/144 (20%, rounded up) and at least 30 visual format trials, targeting all 45 rows.
- Browser validation: Playwright/Chromium against a local `docs/` server, isolated contexts, target viewports, both color schemes, captured console/page errors, and exact-URL-origin request/WebSocket/service-worker ledgers proving default browser-mode requests stay on origin. Report Chromium-only coverage.
- Visual validation: inspect every individual format screenshot plus modal/workflow/Companion screenshots; contact sheets do not replace individual-image inspection.
- Offline validation: interrogate `CacheStorage`, service-worker registrations/controllers/messages, `localStorage` version state, offline reload behavior, and stale cache keys in clean contexts.
- Companion validation: locked current-source build/tests, API/UI E2E against temp config/root/token, watcher/payload/truncation/config assertions, negative auth/CORS/path/symlink/nested-root tests that also prove unchanged disk state, exact request ledger, port/listener inspection, and unconditional process/temp cleanup.
- Final repository gate: `./scripts/check.sh` in full mode, followed by `git status --short` and targeted diffs for all generated artifacts.
- Completion threshold: all release-blocking/high-impact defects fixed and independently reviewed where security-sensitive; all mandatory evidence present; both numerical coverage floors met; all exact final gates green; the predefined no-go rules not triggered; no tag/publish/push/remote mutation performed.

## Risks

- **Cache contamination can create false offline passes.** Mitigation: unique fresh browser contexts, explicit service-worker/cache cleanup, separate limited/full runs, and cache-key assertions.
- **A screenshot can hide delayed renderer failures or blank nested canvases.** Mitigation: wait for type-specific landmarks and stable dimensions, capture console/page errors, inspect actual preview/canvas/image contents, and retain individual shots.
- **The 45 selected examples can overrepresent tiny synthetic fixtures.** Mitigation: prioritize distinct render paths and supplement with richer checked-in examples where a tiny fixture cannot demonstrate layout/scrolling meaningfully; disclose fixture limitations.
- **Visual coverage can become shallow across many formats.** Mitigation: require written per-shot observations and explicit failure modes, not only generated image files.
- **Offline service-worker version testing can accidentally modify release files.** Mitigation: test a copied temporary `docs/` tree or a controlled response shim; never leave a synthetic version in tracked source.
- **Companion testing is locally destructive by design.** Mitigation: generated temp root/config/token, path assertions before every delete, root-delete refusal proof, port ownership check, and unconditional cleanup.
- **CORS is not authentication for local programs.** Mitigation: report it as a browser-origin boundary, separately test token requirements for mutations, and avoid overstating the security model.
- **WSL2 may not represent packaged Linux/Windows desktop behavior.** Mitigation: attempt real Tauri launch, report the exact gap, and do not translate source/build success into tray/native-picker runtime coverage.
- **Chromium can miss Firefox/WebKit regressions and browser-specific private-network rules.** Mitigation: report the engine limitation, supplement when feasible, and keep production HTTPS-to-loopback behavior as an explicit risk unless directly proven.
- **A hostile CORS request can mutate state even when its response is unreadable.** Mitigation: negative tests assert server status and unchanged disposable filesystem/config, not merely browser read failure.
- **The full gate is long and may expose unrelated pre-existing failures.** Mitigation: capture baseline/final results, isolate reproducible failures, and fix only release-impacting in-scope defects while reporting other risks.
- **Generated artifacts can mask source drift.** Mitigation: record pre/post diffs and run generators plus final full gate from the final source tree.

## Open Questions

**Execution gate (blocking):** after this reviewed revision is presented, should Codex execute now, or keep planning? No tests or product changes begin until the user explicitly chooses execution.
