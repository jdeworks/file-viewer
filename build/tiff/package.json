{
  "name": "file-viewer-tiff-build",
  "private": true,
  "description": "Bundles UTIF.js (MIT) into docs/vendor/utif/utif.esm.js. Run: cd build/tiff && npm install && node build.mjs",
  "type": "module",
  "dependencies": {
    "utif": "^3.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
