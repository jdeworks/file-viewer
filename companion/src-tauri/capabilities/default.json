{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Tray-only companion: dialog (folder picker) + autostart, invoked from Rust.",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "dialog:default",
    "autostart:default"
  ]
}
