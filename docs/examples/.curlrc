# curl default configuration

# Follow redirects
location
max-redirs = 10

# Retry
retry = 3
retry-delay = 2
retry-max-time = 60

# Timeouts
max-time = 30
connect-timeout = 10

# HTTP
compressed
http2
user-agent = Mozilla/5.0 (compatible; curl/8.5.0)

# Extra headers
header = "Accept-Language: en-US,en;q=0.9"

# Authentication (value masked in viewer)
user = apiuser:secretpass

# Proxy
proxy = http://proxy.corp.example.com:3128
noproxy = localhost,127.0.0.1,.corp.example.com

# Security
capath = /etc/ssl/certs/

# Progress / output
silent
show-error
