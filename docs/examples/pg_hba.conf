# TYPE  DATABASE        USER            ADDRESS                 METHOD
# "local" connections use Unix domain sockets
local   all             all                                     peer
# IPv4 local connections:
host    all             all             127.0.0.1/32            scram-sha-256
# IPv6 local connections:
host    all             all             ::1/128                 scram-sha-256
# Allow replication connections from localhost
local   replication     all                                     peer
host    replication     all             127.0.0.1/32            scram-sha-256
# Application-specific connections
host    myappdb         appuser         10.0.1.0/24             scram-sha-256
host    myappdb         readonly        10.0.0.0/8              md5
# Admin access from office network
host    all             postgres        192.168.1.0/24          scram-sha-256
