version: "3.9"
services:
  web:
    build: .
    ports:
      - "8080:80"
    depends_on:
      - api
  api:
    image: node:20-alpine
    environment:
      - NODE_ENV=production
    ports:
      - "3000:3000"
    volumes:
      - ./api:/app
volumes:
  data: {}
