- rule: Terminal shell in container
  desc: A shell was spawned by a non-shell program in a container.
  condition: >
    spawned_process and container
    and shell_procs and proc.tty != 0
    and container_entrypoint
    and not user_expected_terminal_shell_in_container_conditions
  output: >
    A shell was spawned in a container with an attached terminal
    (evt.type=%evt.type user=%user.name user_loginuid=%user.loginuid %container.info
    shell=%proc.name parent=%proc.pname cmdline=%proc.cmdline terminal=%proc.tty
    container_id=%container.id image=%container.image.repository)
  priority: NOTICE
  tags: [container, shell, mitre_execution]

- rule: Write below binary dir
  desc: An attempt to write to any file below a set of binary directories.
  condition: >
    bin_dir and evt.dir = < and open_write
    and not package_mgmt_procs
    and not exe_running_docker_save
    and not python_running_get_pip
  output: >
    File below a known binary directory opened for writing
    (evt.type=%evt.type user=%user.name command=%proc.cmdline file=%fd.name
    parent=%proc.pname pcmdline=%proc.pcmdline gparent=%proc.aname[2]
    container_id=%container.id image=%container.image.repository)
  priority: ERROR
  tags: [filesystem, mitre_persistence]

- macro: spawned_process
  condition: evt.type in (execve, execveat) and evt.dir=<

- macro: container
  condition: container.id != host

- list: shell_binaries
  items: [ash, bash, csh, ksh, sh, tcsh, zsh, dash]

- rule: Disallowed SSH Connection
  desc: Detect any outbound SSH connection from a container.
  condition: >
    outbound and evt.type in (sendmsg, sendto)
    and fd.sport=22
    and container
    and not proc.name in (sshd, ssh-keyscan)
  output: >
    Disallowed outbound SSH connection
    (evt.type=%evt.type connection=%fd.name container_id=%container.id
    image=%container.image.repository user=%user.name cmdline=%proc.cmdline)
  priority: CRITICAL
  tags: [network, syscall, mitre_lateral_movement]
