name: my-tool
version: '1.2.3'
summary: A useful command-line tool
description: |
  This snap provides a useful command-line tool
  for processing files efficiently.
grade: stable
confinement: strict
base: core22
apps:
  my-tool:
    command: bin/my-tool
    plugs:
      - home
      - network
parts:
  my-tool:
    plugin: go
    source: .
    build-snaps:
      - go/1.21/stable
plugs:
  home:
  network:
