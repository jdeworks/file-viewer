# 3D Models

> Interactive 3D model viewer with orbit and mesh coloring controls for mesh formats, plus a structured 3MF package preview for print-container metadata, materials, objects, and thumbnails.

## Format Details

| Format | Extensions | Common use |
|--------|-----------|-----------|
| STL | `.stl` | 3D printing, CAD export |
| OBJ | `.obj` + `.mtl` | General-purpose mesh exchange |
| PLY | `.ply` | Point clouds, scan data |
| glTF / GLB | `.gltf`, `.glb` | Web 3D, games, AR/VR |
| 3MF | `.3mf` | 3D printing container with color, materials, metadata, and thumbnails |

## Capabilities Matrix

### View
| Capability | Status | Notes |
|------------|--------|-------|
| Canvas mesh preview | ✅ | STL, OBJ, PLY, and glTF/GLB use orbit via mouse / touch; 3MF currently uses a package summary view |
| Auto-fit | ✅ | Model scaled and centered on load |
| Selection coloring | ✅ | Region, face, and group color modes |
| Colored PLY download | ✅ | Exports current mesh colors from the preview toolbar |
| OBJ / MTL download | ✅ | Mesh preview toolbar can download a colored OBJ plus companion MTL |
| Triangle / vertex count | ✅ | Shown where the format parser exposes it |
| Bounding box dimensions | ✅ | Size in model units displayed in the preview summary |
| OBJ source view | ✅ | OBJ is text — Monaco editor + text diff available |
| glTF JSON source view | ❌ | glTF/GLB route is preview-only |
| GLB binary | ❌ | Raw view not available for binary GLB |
| STL / PLY / 3MF raw view | ❌ | Binary formats — no raw text view |
| Metadata | ✅ | Triangle count, vertex count, material count, bounding box |
| 3MF package view | ✅ | Shows title/designer metadata, unit, objects, base materials, and embedded thumbnail when present |

### Edit
| Capability | Status | Notes |
|------------|--------|-------|
| OBJ source editing | ✅ | Full Monaco editor on OBJ text |
| Preview color edits | ✅ | Temporary in-browser coloring for inspection/export |
| 3D mesh editing | ❌ | No in-browser vertex/face editing |

### Export
| Capability | Status | Notes |
|------------|--------|-------|
| Download original | ✅ | Always available |
| STL → OBJ | ✅ | Geometry-only (no material) |
| STL → PLY | ✅ | ASCII PLY |
| OBJ → STL | ✅ | ASCII STL |
| OBJ → PLY | ✅ | ASCII PLY |
| PLY → STL | ✅ | ASCII STL |
| PLY → OBJ | ✅ | Geometry-only OBJ |
| glTF/GLB → OBJ | ✅ | Geometry-only OBJ from the parsed mesh preview |
| glTF/GLB → PLY | ✅ | ASCII PLY from the parsed mesh preview |
| Colored preview → PLY | ✅ | Toolbar export includes selected preview colors |
| Colored preview → OBJ/MTL | ✅ | Toolbar export includes selected preview colors as generated materials |
| Export as GLTF/GLB | ❌ | Not yet implemented |

## Real-World Examples

- [`sample.stl`](../examples/sample.stl) — example STL 3D model
- [`sample.obj`](../examples/sample.obj) — example OBJ mesh with material
- [`sample.glb`](../examples/sample.glb) — example GLB scene
- [`sample.3mf`](../examples/sample.3mf) — example 3MF print container

## Known Limitations

- Textures in OBJ (MTL references) are not loaded — models appear as simple colored geometry
- Animations in glTF are not played back
- 3MF mesh geometry is not rendered in the orbit canvas yet; use the dedicated 3MF page for current package-level support
- Very large meshes (>1M triangles) may be slow to parse and render
- No lighting beyond a fixed ambient + directional light setup

## Gap Analysis

| Feature | Priority | Difficulty | Notes |
|---------|----------|------------|-------|
| Material / texture support | High | Hard | Load MTL and texture images for OBJ; PBR for glTF |
| Better camera controls | Med | Med | Add zoom and pan without making touch interactions fragile |
| Export as glTF/GLB | Med | Med | Convert any mesh to the modern glTF format |
| Point cloud rendering | Med | Med | PLY point clouds shown as dots instead of faces |
| Section cut (slice view) | Low | Hard | Cross-section plane for 3D-print inspection |
